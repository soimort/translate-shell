version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: |
            apt-get update
            apt-get -y install make gawk rlwrap emacs
      - run:
          name: Sanity Check
          command: make check
